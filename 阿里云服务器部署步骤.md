# 阿里云服务器部署步骤（当前服务器实测版）

本文用于把“我的账本”本地最新代码更新到当前阿里云服务器。

当前已确认的信息：

- 项目目录：`E:\sharkapp\shark-ledger-main`
- 阿里云公网地址：`http://8.133.219.98/`
- 服务器系统：Ubuntu
- 登录用户：`root`
- Web 服务：Nginx
- **Nginx 实际站点目录：`/opt/my-ledger/dist`**
- 上传包暂存目录：`/root`
- GitHub 仓库可以继续保持私有，不需要为部署而公开

> 最重要的一点：不要把新版解压到 `/var/www/html`。这台服务器实际展示的是 `/opt/my-ledger/dist` 中的文件。

## 一、数据是否安全

更新部署只会替换网页代码，不会主动修改或删除：

- Supabase 中的云端账单、账户、还款计划、6211 规划和出差记录；
- 手机或电脑浏览器中已经保存的本地数据；
- Supabase 登录账号。

为了避免误操作，不要删除 Supabase 表，不要清除手机浏览器网站数据，也不要在确认同步前重新录入一套重复数据。

## 二、本地修改完成后先检查

在 Windows PowerShell 中进入项目目录：

```powershell
cd E:\sharkapp\shark-ledger-main
```

启动本地预览：

```powershell
npm run dev
```

浏览器打开：

```text
http://127.0.0.1:5173/
```

确认新增功能、页面跳转和数据读取都正常后，再制作部署包。

云同步版本在打包前必须保留项目根目录中的 `.env.local`。只检查文件是否存在，不要把其中的地址或密钥发给别人：

```powershell
Test-Path .env.local
```

正常结果应为：

```text
True
```

## 三、生成网页文件

首次打包或依赖有变化时先执行：

```powershell
npm install
```

检查 TypeScript：

```powershell
npx tsc --noEmit
```

没有红色错误后执行阿里云服务器生产构建：

```powershell
npm run build:server
```

构建文件位于：

```text
E:\sharkapp\shark-ledger-main\dist
```

至少确认下面两项存在：

```powershell
Test-Path .\dist\index.html
Test-Path .\dist\assets
```

两条结果都应为 `True`。

### 为什么服务器部署使用 `build:server`

当前公网 IP 使用 HTTP，无法启用完整的 PWA Service Worker。`build:server` 会正常生成网站文件，但不生成 Service Worker，同时避开当前 Windows 环境可能出现的：

```text
Dynamic require of "workbox-build" is not supported
```

这是生成 PWA 离线缓存文件时的兼容问题，不影响网站与 Supabase 功能。用于其他 HTTPS/PWA 发布环境时，仍可执行原来的 `npm run build`。

## 四、制作 ZIP 部署包

在项目目录执行：

```powershell
Compress-Archive -Path .\dist\* -DestinationPath .\shark-ledger-deploy.zip -Force
```

确认压缩包存在：

```powershell
Get-Item .\shark-ledger-deploy.zip
```

正确的 ZIP 打开后，第一层应直接看到 `index.html`、`assets` 等文件，不能在里面再套一层 `dist` 文件夹。

部署 ZIP 只是临时文件，不要提交到 GitHub。

## 五、上传到阿里云服务器

使用阿里云控制台的文件管理功能，把：

```text
E:\sharkapp\shark-ledger-main\shark-ledger-deploy.zip
```

上传到服务器：

```text
/root/shark-ledger-deploy.zip
```

上传后在服务器终端确认：

```bash
ls -lh /root/shark-ledger-deploy.zip
```

如果能看到文件大小和日期，说明上传成功。

## 六、服务器首次准备

如果执行 `unzip` 时出现 `Command 'unzip' not found`，依次执行：

```bash
apt update
```

```bash
apt install -y unzip
```

若安装过程中出现“Which services should be restarted?”：不用勾选列表，按 `Tab` 移动到 `<Ok>`，再按 `Enter`。

已经安装过 `unzip` 就跳过本节。

## 七、检查压缩包后正式更新

先测试 ZIP 是否完整：

```bash
unzip -t /root/shark-ledger-deploy.zip
```

最后应显示没有错误。只有检查通过后才继续。

把新版解压到这台服务器真正使用的网站目录：

```bash
unzip -o /root/shark-ledger-deploy.zip -d /opt/my-ledger/dist
```

`-o` 表示覆盖同名旧网页文件。它不会操作 Supabase 数据。

检查 Nginx 配置：

```bash
nginx -t
```

必须看到类似下面两行：

```text
syntax is ok
test is successful
```

重新加载 Nginx：

```bash
systemctl reload nginx
```

## 八、必须执行的双重版本校验

先查看刚解压的首页引用了哪个主程序文件：

```bash
grep -o 'assets/index-[^" ]*\.js' /opt/my-ledger/dist/index.html
```

再查看 Nginx 实际返回了哪个主程序文件：

```bash
curl -s http://127.0.0.1/ | grep -o 'assets/index-[^" ]*\.js'
```

两条命令的输出必须完全相同。例如本次部署成功时，两边都是：

```text
assets/index-D50BLxi1.js
```

以后重新构建时，`D50BLxi1` 这一段通常会变化；只需要确认两条命令输出一致。

如果不一致，说明文件解压到了错误目录，或者 Nginx 使用了其他目录。执行：

```bash
nginx -T 2>/dev/null | grep -n 'root '
```

当前服务器的正确结果包含：

```text
root /opt/my-ledger/dist;
```

## 九、手机打开新版

先关闭手机浏览器中原来的网页标签，再直接打开：

```text
http://8.133.219.98/?v=本次更新时间
```

例如：

```text
http://8.133.219.98/?v=20260906
```

`?v=` 后面的内容用于避开旧网页缓存，每次部署时换成当天日期或新的数字即可。

如果普通标签仍显示旧界面，先用浏览器无痕模式打开同一地址。不要急着清除网站数据，因为清除网站数据也会删除这个地址下尚未同步的本地数据。

## 十、确认云端数据

新版界面确认无误后：

1. 登录与原设备完全相同的 Supabase 账号；
2. 输入相同的数据口令；
3. 点击“立即同步”；
4. 检查账单、资产、还款计划、6211 规划和出差记录是否一致。

如果界面已经是新版但数据不同，这是浏览器本地数据不同，不是部署失败。完成同账号、同口令的 Supabase 同步即可。

## 十一、以后每次更新只执行这些步骤

1. 本地完成并测试代码；
2. 执行 `npx tsc --noEmit`；
3. 执行 `npm run build:server`；
4. 把 `dist` 内容压缩成 `shark-ledger-deploy.zip`；
5. 上传 ZIP 到服务器 `/root`；
6. 执行 `unzip -t` 检查压缩包；
7. 解压到 `/opt/my-ledger/dist`；
8. 执行 `nginx -t` 和 `systemctl reload nginx`；
9. 比较磁盘首页与 Nginx 返回的两个 `index-*.js` 文件名；
10. 手机使用带新 `?v=` 数字的公网地址打开并检查。

## 十二、常见问题

### 1. 手机能打开，但没有新功能

先执行第八节的两条版本校验命令。两条结果不一致时，不要继续清理手机缓存，先修正服务器解压目录。

### 2. 页面完全无法打开

执行：

```bash
systemctl status nginx --no-pager
```

```bash
ss -ltnp | grep ':80'
```

同时检查阿里云 ECS 安全组是否已放行入方向 TCP 80 端口。

### 3. 页面能打开，但 Supabase 无法登录或同步

确认本地打包前 `.env.local` 存在，而且使用的是正确的 Supabase 项目配置。不要在聊天、截图、GitHub 或服务器公开目录中发送或保存私密口令。

### 4. 公网 IP 页面为什么显示“不安全”

因为当前使用的是 HTTP 公网 IP，没有域名和 HTTPS。这不代表部署失败。网站和 Supabase HTTPS 接口可以使用，但完整 PWA 安装、离线缓存等能力需要后续配置域名和 HTTPS。

### 5. 是否需要公开 GitHub 仓库

不需要。ZIP 上传部署与仓库公开或私有没有关系，代码仓库可以继续保持私有。
